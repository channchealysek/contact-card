{
  "name": "contact-card",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "heroku-prebuild": "echo This runs before Heroku installs dependencies.",
    "start:dev": "concurrently \"cd server && npm run server\" \"cd client && npm run dev\"",
    "start": "npm run build && cd server && node server.js",
    "server": "cd server nodemon server.js --ignore client",
    "build": "cd client && npm run build",
    "client": "cd client && npm start",
    "heroku-postbuild": "echo This runs after Heroku installs dependencies, but before Heroku prunes and caches dependencies.",
    "heroku-cleanup": "echo This runs after Heroku prunes and caches dependencies."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/channchealysek/contact-card.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/channchealysek/contact-card/issues"
  },
  "homepage": "https://github.com/channchealysek/contact-card#readme",
  "dependencies": {
    "concurrently": "^7.4.0"
  },
  "engines": {
    "node": "18.x",
    "npm": "8.x"
  }
}
